/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["cdn.prod.website-files.com", "images.unsplash.com", 'imgs.search.brave.com'],
    },
  };
  
  export default nextConfig;
  